public class Teste extends TestCase {
    public void teste() {
        // Jogo jogo = new Jogo();
        // jogo.setNome("Jogo");
        // jogo.setPreco(10.0);
        // jogo.setQuantidade(10);
        // jogo.setTipo("RPG");
        // assertEquals("Jogo", jogo.getNome());
        // assertEquals(10.0, jogo.getPreco(), 0.0);
        // assertEquals(10, jogo.getQuantidade());
        // assertEquals("RPG", jogo.getTipo());
    }
}
    

